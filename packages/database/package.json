{
  "name": "@artiverges/database",
  "version": "0.1.0",
  "private": true,
  "description": "ARTIVERGES NEXT — Prisma schema and generated database client",
  "main": "index.ts",
  "scripts": {
    "generate": "prisma generate",
    "validate": "prisma validate",
    "format": "prisma format",
    "db:pull": "prisma db pull",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
